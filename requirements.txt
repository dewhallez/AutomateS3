boto3==1.24.95
botocore==1.27.95
s3transfer==0.6.0
python-dateutil==2.8.2
jmespath==1.0.1
urllib3==1.26.12

# Notes:
# - These versions match the packages present in the project's virtualenv under `env/lib/.../site-packages`.
# - Installing with `pip install -r requirements.txt` will install boto3 and its core dependencies.